{
  "defaultMode": "bypassPermissions",
  "permissions": {
    "allow": ["Bash", "Read", "Write", "Edit", "Grep", "Glob", "Task", "WebFetch", "WebSearch"]
  },
  "enabledPlugins": [{{range $i, $lsp := .LSPs}}{{if $i}}, {{end}}"{{$lsp.Plugin | jsonString}}"{{end}}]
}
