version: 2

builds:
  - main: .
    binary: ccbox
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/bjro/ccbox/cmd.version={{.Version}}

archives:
  - formats:
      - tar.gz
    name_template: "ccbox_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^ci:"

brews:
  - repository:
      owner: bjro
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/BjRo/ccbox
    description: Generate devcontainer setups for Claude Code
    license: MIT
    install: |
      bin.install "ccbox"
    test: |
      system "#{bin}/ccbox", "--version"
